<Window x:Class="WpfDISample.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDISample.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 1行目: ラベルとコンボボックス -->
        <Label Content="名前：" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="10"/>
        <ComboBox ItemsSource="{Binding Names}" SelectedItem="{Binding SelectedName}" Grid.Row="0" Grid.Column="1" Margin="10"/>

        <!-- 2行目: ボタン -->
        <Button Content="Get Names" Command="{Binding GetNamesCommand}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="10" Width="100"/>

        <!-- 3行目: Show Greeting ボタン -->
        <Button Content="Show Greeting" Command="{Binding ShowGreetingCommand}" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="10" Width="100"/>

        <!-- 4行目: テキストボックス -->
        <TextBox Text="{Binding Greeting, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" Height="23" Margin="10" TextWrapping="Wrap" VerticalAlignment="Top" Width="480"/>
    </Grid>
</Window>
